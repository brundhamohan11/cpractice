QUESTION:
Write a C program to reverse an array.

#include <stdio.h>
int main() {
    int n, i;
    scanf("%d", &n);
    int arr[n], rev[n];
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    for(i = 0; i < n; i++) {
        rev[i] = arr[n - 1 - i];
    }
    for(i = 0; i < n; i++) {
        printf("Reversed array is:\n" );
        for(i=0;i<n;i++){
        printf("%d\n",rev[i]);
    }
    }
    
    return 0;
}
