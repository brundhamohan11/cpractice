QUESTION:
Write a C program to find the maximum element in an array.


#include<stdio.h>
#include<limits.h>
int main()
{
    int size;
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    int maximum=arr[0];
    for(int i=0;i<size;i++){
        if(arr[i]>maximum)
        maximum =arr[i];
    }
    printf("%d",maximum);
    return 0;
}
