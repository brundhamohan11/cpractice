QUESTION:
Write a C program to generate the following series 0, 3, 8, 15, 24….





#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int first=0,next=3;
    for(int i=1;i<=n;i++){
        printf("%d ",first);
        first+=next;
        next+=2;
    }

	return 0;
}
